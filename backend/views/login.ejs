<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Giriş Yap - Kara Kedi Mobilya</title>
    <style>
        .alert {
            padding: 15px;
            margin-bottom: 20px;
            border: 1px solid transparent;
            border-radius: 4px;
        }
        .alert-danger {
            color: #721c24;
            background-color: #f8d7da;
            border-color: #f5c6cb;
        }
    </style>
</head>

<body class="login-page">
    <!--Navbar-->
    <nav>
        <div class="nav-left">
            <h1 class="brand-name">Kara Kedi Mobilya</h1>
        </div>
        <div class="nav-center">
            <ul>
                <li><a href="/">Ana Sayfa</a></li>
                <li><a href="/cart">Sepetim</a></li>
            </ul>
        </div>
        <div class="nav-right">
            <% if (user) { %>
                <span class="welcome-msg">Hoş geldiniz, <%= user.name %></span>
                <a href="/logout" class="logout-btn">Çıkış Yap</a>
            <% } else { %>
                <a href="/login" class="login-btn active">Giriş Yap</a>
                <a href="/register" class="register-btn">Kayıt Ol</a>
            <% } %>
        </div>
    </nav>

    <!--Giriş Yap-->
    <div class="login-container">
        <h1>Giriş Yap</h1>
        
        <% if (messages.error) { %>
            <div class="alert alert-danger"><%= messages.error %></div>
        <% } %>

        <form action="/login" method="POST">
            <div class="form-group">
                <input placeholder="Email" type="email" id="email" name="email" required>
            </div>
            
            <div class="form-group">
                <input placeholder="Şifre" type="password" id="password" name="password" required>
            </div>

            <button type="submit" class="btn-login">Giriş Yap</button>

            <p class="register-link">Henüz bir hesabın yok mu? <a href="/register">Kayıt Ol</a></p>
        </form>
    </div>
</body>
</html>